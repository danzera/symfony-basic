{% extends 'base.html.twig' %}

{% block title %}VictoriusKangaroo index{% endblock %}

{% block body %}
    <h1>VictoriusKangaroo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Arms</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for victorius_kangaroo in victorius_kangaroos %}
            <tr>
                <td>{{ victorius_kangaroo.id }}</td>
                <td>{{ victorius_kangaroo.arms }}</td>
                <td>
                    <a href="{{ path('victorius_kangaroo_show', {'id': victorius_kangaroo.id}) }}">show</a>
                    <a href="{{ path('victorius_kangaroo_edit', {'id': victorius_kangaroo.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('victorius_kangaroo_new') }}">Create new</a>
{% endblock %}
